import { CTA_TRUST_BADGES } from "@/constants/content";
import SharedCTA from "@brand/shared/components/cta";

const CTA = () => {
  return <SharedCTA trustBadges={CTA_TRUST_BADGES} />;
};

export default CTA;
