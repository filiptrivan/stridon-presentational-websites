# Set automatically via next.config.ts
# NEXT_PUBLIC_BRAND_SLUG=stridon

# Backend API (server-only — required even if product routes aren't used)
API_URL=

# Brevo email (contact + B2B forms) — wire when ready
BREVO_API_KEY=

# Sentry error tracking
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=
